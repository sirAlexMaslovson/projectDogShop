export function FavoritesPage() {
  return (
    <h1>Любимки</h1>
  )
}

/*    <div className={formStyles.cart}>
        <div className="container d-flex flex-wrap pt-2 justify-content-between">
          {data.map((post) => (
          // eslint-disable-next-line no-underscore-dangle
            <div className={`card m-3 ${formStyles.pageCard}`} style={{ width: '18rem' }}
             key={post._id}>
              <img src={post.pictures} className="card-img-top" alt={post.name} />
              <div className="card-body">
                <h5 className="card-title">{post.name}</h5>
              </div>
              <h5 className="card-text text-center">
                {post.price}
                {' '}
                ₽
              </h5>
            </div>
          ))}
        </div>
      </div> */
